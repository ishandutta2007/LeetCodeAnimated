## LeetCode Animation management script anima usage instructions

Requires Python 3.6+ version

Install dependencies:

```
pip install -r requirements.txt
```

### Create new solution

Generate problem solution directory framework and Markdown file template.

```
python anima.py new 1 'Two Sum'
```

